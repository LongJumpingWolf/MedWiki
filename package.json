{
  "name": "medwiki",
  "version": "1.0.0",
  "private": true,
  "description": "A personal medical textbook: static site plus a tiny local save server.",
  "scripts": {
    "start": "node serve.js",
    "test": "node tests/smoke.mjs",
    "bump": "node tools/bump.mjs",
    "publish": "git add -A && git commit -m \"Update notes\" && git push"
  },
  "engines": {
    "node": ">=22"
  }
}
